import { OverDueTaskDirective } from './over-due-task.directive';

describe('OverDueTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new OverDueTaskDirective();
    expect(directive).toBeTruthy();
  });
});
