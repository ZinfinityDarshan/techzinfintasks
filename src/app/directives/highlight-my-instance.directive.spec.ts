import { HighlightMyInstanceDirective } from './highlight-my-instance.directive';

describe('HighlightMyInstanceDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightMyInstanceDirective();
    expect(directive).toBeTruthy();
  });
});
